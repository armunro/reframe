<Application x:Class="Reframe.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Dark" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>

            <!-- ======================================================== -->
            <!-- Desaturated Dark Navy Palette (Base: rgb(30, 32, 48) / #1E2030) -->
            <!-- ======================================================== -->
            <SolidColorBrush x:Key="ApplicationBackgroundBrush" Color="#181924" />
            <SolidColorBrush x:Key="SolidBackgroundFillColorBaseBrush" Color="#181924" />
            <SolidColorBrush x:Key="SolidBackgroundFillColorSecondaryBrush" Color="#181924" />
            <SolidColorBrush x:Key="SolidBackgroundFillColorTertiaryBrush" Color="#15161F" />
            <SolidColorBrush x:Key="SolidBackgroundFillColorQuarternaryBrush" Color="#121319" />

            <!-- Surface Brushes -->
            <SolidColorBrush x:Key="SurfaceBackgroundBrush" Color="#1E2030" />
            <SolidColorBrush x:Key="SurfaceHeaderBackgroundBrush" Color="#242735" />
            <SolidColorBrush x:Key="SurfaceDarkBackgroundBrush" Color="#181924" />
            <SolidColorBrush x:Key="SurfaceCardBackgroundBrush" Color="#242735" />

            <SolidColorBrush x:Key="CardBackgroundFillColorDefaultBrush" Color="#242735" />
            <SolidColorBrush x:Key="CardBackgroundFillColorSecondaryBrush" Color="#1E2030" />
            <SolidColorBrush x:Key="CardBackgroundFillColorTertiaryBrush" Color="#181924" />

            <SolidColorBrush x:Key="ControlFillColorDefaultBrush" Color="#232635" />
            <SolidColorBrush x:Key="ControlFillColorSecondaryBrush" Color="#282C3C" />
            <SolidColorBrush x:Key="ControlFillColorTertiaryBrush" Color="#1E212E" />
            <SolidColorBrush x:Key="ControlFillColorDisabledBrush" Color="#181A24" />

            <SolidColorBrush x:Key="ControlStrongFillColorDefaultBrush" Color="#2C3142" />

            <SolidColorBrush x:Key="ControlStrokeColorDefaultBrush" Color="#343849" />
            <SolidColorBrush x:Key="ControlStrokeColorSecondaryBrush" Color="#465472" />
            <SolidColorBrush x:Key="ControlElevationBorderBrush" Color="#343849" />
            <SolidColorBrush x:Key="CardStrokeColorDefaultBrush" Color="#343849" />
            <SolidColorBrush x:Key="SurfaceStrokeColorDefaultBrush" Color="#343849" />
            <SolidColorBrush x:Key="DividerStrokeColorDefaultBrush" Color="#2B2E3C" />

            <SolidColorBrush x:Key="TextFillColorPrimaryBrush" Color="#DCE1EB" />
            <SolidColorBrush x:Key="TextFillColorSecondaryBrush" Color="#A6ADB9" />
            <SolidColorBrush x:Key="TextFillColorTertiaryBrush" Color="#727888" />
            <SolidColorBrush x:Key="TextFillColorDisabledBrush" Color="#525866" />
            <SolidColorBrush x:Key="MutedTextBrush" Color="#828896" />
            <SolidColorBrush x:Key="DimTextBrush" Color="#646A78" />

            <!-- Calmer Slate-Blue Accents -->
            <SolidColorBrush x:Key="SystemAccentColorBrush" Color="#668BC4" />
            <SolidColorBrush x:Key="SystemAccentColorSecondaryBrush" Color="#465E8A" />
            <SolidColorBrush x:Key="SystemAccentColorTertiaryBrush" Color="#56A5B8" />

            <SolidColorBrush x:Key="AccentSuccessBrush" Color="#4EC9B0" />
            <SolidColorBrush x:Key="AccentWarningBrush" Color="#CE9178" />
            <SolidColorBrush x:Key="AccentInfoBrush" Color="#7DCFFF" />
            <SolidColorBrush x:Key="AccentBlueBadgeBrush" Color="#007ACC" />

            <!-- Tab Brushes -->
            <SolidColorBrush x:Key="TabStripBackgroundBrush" Color="#181A24" />
            <SolidColorBrush x:Key="TabItemUnselectedBackgroundBrush" Color="#202330" />
            <SolidColorBrush x:Key="TabItemHoverBackgroundBrush" Color="#272B3B" />
            <SolidColorBrush x:Key="TabItemSelectedBackgroundBrush" Color="#242735" />

            <!-- Custom Mono Font for Developer Text Boxes -->
            <FontFamily x:Key="EditorFontFamily">Cascadia Code, Consolas, Courier New, monospace</FontFamily>

            <!-- ======================================================== -->
            <!-- Global / Shared Control Styles -->
            <!-- ======================================================== -->

            <!-- High-Visibility TabControl Style -->
            <Style TargetType="{x:Type TabControl}">
                <Setter Property="Padding" Value="0" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type TabControl}">
                            <Grid KeyboardNavigation.TabNavigation="Local">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <!-- Tab Header Strip with dark backing and bottom separator -->
                                <Border Grid.Row="0"
                                        Background="{DynamicResource TabStripBackgroundBrush}"
                                        BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                                        BorderThickness="0,0,0,1"
                                        Padding="6,5,6,0"
                                        SnapsToDevicePixels="True">
                                    <TabPanel IsItemsHost="True"
                                              KeyboardNavigation.TabIndex="1" />
                                </Border>
                                <!-- Tab Content Host -->
                                <Border Grid.Row="1"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter x:Name="PART_SelectedContentHost"
                                                      ContentSource="SelectedContent"
                                                      Margin="{TemplateBinding Padding}"
                                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                </Border>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Clear, High-Contrast TabItem Style with Active Indicator -->
            <Style TargetType="{x:Type TabItem}">
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
                <Setter Property="Background" Value="{DynamicResource TabItemUnselectedBackgroundBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}" />
                <Setter Property="BorderThickness" Value="1,1,1,0" />
                <Setter Property="Margin" Value="0,0,3,0" />
                <Setter Property="Padding" Value="12,6" />
                <Setter Property="FontSize" Value="11" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type TabItem}">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4,4,0,0"
                                    Margin="{TemplateBinding Margin}"
                                    Padding="{TemplateBinding Padding}"
                                    SnapsToDevicePixels="True">
                                <Grid>
                                    <ContentPresenter x:Name="contentPresenter"
                                                      ContentSource="Header"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      RecognizesAccessKey="True"
                                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource TabItemHoverBackgroundBrush}" />
                                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource TabItemSelectedBackgroundBrush}" />
                                    <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource SystemAccentColorBrush}" />
                                    <Setter Property="Foreground" Value="#FFFFFF" />
                                    <Setter Property="FontWeight" Value="Bold" />
                                    <Setter Property="Panel.ZIndex" Value="10" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Opacity" Value="0.35" />
                                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorDisabledBrush}" />
                                    <Setter Property="Cursor" Value="No" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Send to Input Green Accent Button Style -->
            <Style x:Key="SendToInputButtonStyle" TargetType="ui:Button">
                <Setter Property="Appearance" Value="Success" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Padding" Value="14,5" />
                <Setter Property="Cursor" Value="Hand" />
            </Style>

            <!-- Section Expander Style -->
            <Style x:Key="SectionExpanderStyle" TargetType="Expander">
                <Setter Property="IsExpanded" Value="False" />
                <Setter Property="Foreground" Value="{DynamicResource AccentInfoBrush}" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="FontSize" Value="11" />
                <Setter Property="Margin" Value="0,1,0,3" />
                <Setter Property="Padding" Value="6,5,6,5" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Expander">
                            <Border Background="{TemplateBinding Background}"
                                    BorderThickness="0"
                                    SnapsToDevicePixels="True">
                                <DockPanel>
                                    <ToggleButton x:Name="HeaderSite"
                                                  DockPanel.Dock="Top"
                                                  Content="{TemplateBinding Header}"
                                                  ContentTemplate="{TemplateBinding HeaderTemplate}"
                                                  Foreground="{TemplateBinding Foreground}"
                                                  FontWeight="{TemplateBinding FontWeight}"
                                                  FontSize="{TemplateBinding FontSize}"
                                                  IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border x:Name="headerBorder"
                                                        Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                                                        BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                                                        BorderThickness="1"
                                                        CornerRadius="4"
                                                        Padding="7,4"
                                                        Cursor="Hand"
                                                        SnapsToDevicePixels="True">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*" />
                                                            <ColumnDefinition Width="Auto" />
                                                        </Grid.ColumnDefinitions>
                                                        <ContentPresenter Grid.Column="0"
                                                                          VerticalAlignment="Center"
                                                                          RecognizesAccessKey="True" />
                                                        <Path x:Name="arrow"
                                                              Grid.Column="1"
                                                              HorizontalAlignment="Right"
                                                              VerticalAlignment="Center"
                                                              Margin="6,0,2,0"
                                                              Data="M 0 0 L 4 4 L 8 0 Z"
                                                              Fill="{DynamicResource SystemAccentColorBrush}"
                                                              RenderTransformOrigin="0.5,0.5">
                                                            <Path.RenderTransform>
                                                                <RotateTransform Angle="-90" />
                                                            </Path.RenderTransform>
                                                        </Path>
                                                    </Grid>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="arrow" Property="RenderTransform">
                                                            <Setter.Value>
                                                                <RotateTransform Angle="0" />
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Setter TargetName="arrow" Property="Fill" Value="{DynamicResource AccentSuccessBrush}" />
                                                        <Setter TargetName="headerBorder" Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                                                        <Setter TargetName="headerBorder" Property="BorderBrush" Value="{DynamicResource ControlStrokeColorSecondaryBrush}" />
                                                        <Setter TargetName="headerBorder" Property="CornerRadius" Value="4,4,0,0" />
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="headerBorder" Property="Background" Value="{DynamicResource ControlStrongFillColorDefaultBrush}" />
                                                        <Setter TargetName="headerBorder" Property="BorderBrush" Value="{DynamicResource SystemAccentColorBrush}" />
                                                        <Setter TargetName="arrow" Property="Fill" Value="{DynamicResource SystemAccentColorBrush}" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                    <Border x:Name="ExpandSiteBorder"
                                            DockPanel.Dock="Bottom"
                                            Background="{DynamicResource SolidBackgroundFillColorSecondaryBrush}"
                                            BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                                            BorderThickness="1,0,1,1"
                                            CornerRadius="0,0,4,4"
                                            Margin="0,-1,0,0"
                                            Padding="{TemplateBinding Padding}"
                                            Visibility="Collapsed">
                                        <ContentPresenter x:Name="ExpandSite"
                                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                          Focusable="False" />
                                    </Border>
                                </DockPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsExpanded" Value="True">
                                    <Setter TargetName="ExpandSiteBorder" Property="Visibility" Value="Visible" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorDisabledBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Tool Button Style -->
            <Style x:Key="ToolButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{DynamicResource CardBackgroundFillColorDefaultBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Margin" Value="1,1" />
                <Setter Property="Padding" Value="6,4" />
                <Setter Property="FontSize" Value="11" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="btnBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="3"
                                    Padding="{TemplateBinding Padding}"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="btnBorder" Property="Background" Value="{DynamicResource ControlStrongFillColorDefaultBrush}" />
                                    <Setter TargetName="btnBorder" Property="BorderBrush" Value="{DynamicResource SystemAccentColorBrush}" />
                                    <Setter Property="Foreground" Value="#FFFFFF" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="btnBorder" Property="Background" Value="{DynamicResource SolidBackgroundFillColorSecondaryBrush}" />
                                    <Setter TargetName="btnBorder" Property="BorderBrush" Value="{DynamicResource ControlStrokeColorSecondaryBrush}" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="btnBorder" Property="Opacity" Value="0.5" />
                                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorDisabledBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Secondary Tool Button Style -->
            <Style x:Key="SecondaryToolButtonStyle" TargetType="Button" BasedOn="{StaticResource ToolButtonStyle}">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Margin" Value="1,1" />
                <Setter Property="Padding" Value="4,2" />
                <Setter Property="FontSize" Value="10" />
                <Setter Property="HorizontalContentAlignment" Value="Center" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="btnBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="3"
                                    Padding="{TemplateBinding Padding}"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="btnBorder" Property="Background" Value="{DynamicResource ControlStrongFillColorDefaultBrush}" />
                                    <Setter TargetName="btnBorder" Property="BorderBrush" Value="{DynamicResource SystemAccentColorBrush}" />
                                    <Setter Property="Foreground" Value="#FFFFFF" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="btnBorder" Property="Background" Value="{DynamicResource SolidBackgroundFillColorSecondaryBrush}" />
                                    <Setter TargetName="btnBorder" Property="BorderBrush" Value="{DynamicResource ControlStrokeColorSecondaryBrush}" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="btnBorder" Property="Opacity" Value="0.4" />
                                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorDisabledBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Primary Action Button Style -->
            <Style x:Key="PrimaryToolButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{DynamicResource SystemAccentColorSecondaryBrush}" />
                <Setter Property="Foreground" Value="#FFFFFF" />
                <Setter Property="BorderBrush" Value="{DynamicResource SystemAccentColorBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Margin" Value="1,1" />
                <Setter Property="Padding" Value="6,4" />
                <Setter Property="FontSize" Value="11" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="btnBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="3"
                                    Padding="{TemplateBinding Padding}"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="btnBorder" Property="Background" Value="#5472A5" />
                                    <Setter TargetName="btnBorder" Property="BorderBrush" Value="#7BA3DD" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="btnBorder" Property="Background" Value="#324566" />
                                    <Setter TargetName="btnBorder" Property="BorderBrush" Value="#24334C" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="btnBorder" Property="Opacity" Value="0.5" />
                                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorDisabledBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
